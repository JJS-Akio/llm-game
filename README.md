First if Rust is not installed go to the link: https://rust-lang.org/tools/install/ and install.\
Second, make sure to build with cargo run command: ```cargo build```\
Third run the game: ```cargo run```
